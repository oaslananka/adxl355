name: CI

on:
  push:
    branches: [main, master, develop]
    paths-ignore:
      - "**.md"
      - "docs/**"
  pull_request:
    branches: [main, master]
  workflow_dispatch:
  workflow_call:

concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  # ── C ────────────────────────────────────────────────────────────────
  c:
    name: C (GCC)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: c
    steps:
      - uses: actions/checkout@v6
      - name: Install deps
        run: sudo apt-get update && sudo apt-get install -y cmake gcc
      - name: Configure
        run: cmake -S . -B build -DADXL355_BUILD_TESTS=ON -DADXL355_BUILD_EXAMPLES=ON
      - name: Build
        run: cmake --build build
      - name: Test
        run: ctest --test-dir build --output-on-failure

  # ── C++ ──────────────────────────────────────────────────────────────
  cpp:
    name: C++ (GCC)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cpp
    steps:
      - uses: actions/checkout@v6
      - name: Install deps
        run: sudo apt-get update && sudo apt-get install -y cmake g++
      - name: Build C core first
        run: |
          cmake -S ${{ github.workspace }}/c -B ${{ github.workspace }}/c/build \
            -DADXL355_BUILD_TESTS=OFF -DADXL355_BUILD_EXAMPLES=OFF
          cmake --build ${{ github.workspace }}/c/build
      - name: Configure C++
        run: >
          cmake -S . -B build
          -DADXL355_BUILD_TESTS=ON -DADXL355_BUILD_EXAMPLES=ON
          -DCMAKE_PREFIX_PATH=${{ github.workspace }}/c/build
      - name: Build
        run: cmake --build build
      - name: Test
        run: ctest --test-dir build --output-on-failure

  # ── Python ────────────────────────────────────────────────────────────
  python:
    name: Python
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: python
    strategy:
      matrix:
        python-version: ["3.9", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v6
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: pip install -e .[dev]
      - name: Test
        run: python -m pytest -v

  # ── Rust ──────────────────────────────────────────────────────────────
  rust:
    name: Rust
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: rust
    steps:
      - uses: actions/checkout@v6
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - name: Check
        run: cargo check --all-targets --all-features
      - name: Lint
        run: cargo clippy --all-targets --all-features -- -D warnings
      - name: Test (all features)
        run: cargo test --all-features
      - name: Check (no_std + hal)
        run: cargo check --no-default-features --features hal
      - name: Test (no_std)
        run: cargo test --no-default-features

  # ── Node.js ───────────────────────────────────────────────────────────
  node:
    name: Node.js
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: node
    strategy:
      matrix:
        node-version: ["18", "20", "22"]
    steps:
      - uses: actions/checkout@v6
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"
          cache-dependency-path: node/package-lock.json
      - name: Install
        run: npm ci
      - name: Build
        run: npm run build
      - name: Test
        run: npm test
      - name: Type check
        run: npx tsc --noEmit

  # ── Go ────────────────────────────────────────────────────────────────
  go:
    name: Go
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: go
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.21"
          cache: false
      - name: Test
        run: go test ./...

  # ── Required shared-vector gate ────────────────────────────────────────
  consistency:
    name: Cross-language Consistency
    runs-on: ubuntu-latest
    needs: [c, cpp, python, rust, node, go]
    steps:
      - uses: actions/checkout@v6
      - name: Install native build tools
        run: sudo apt-get update && sudo apt-get install -y cmake gcc g++
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: node/package-lock.json
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.21"
          cache: false
      - name: Install verifier dependencies
        run: python -m pip install -e ./python[dev] pyyaml
      - name: Run required cross-language vector gate
        run: python scripts/verify_vectors.py --ci
      - name: Test release and CI automation
        run: python -m unittest discover -s scripts/tests -p "test_*.py" -v
